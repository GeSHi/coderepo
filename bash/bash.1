#!/bin/bash
# This isn\'t right
echo $# arguments ${#arr[@]} ...
function die {
[ "$1" ] && echo -e "ERROR: $1" > /dev/stderr
[ "$2" == "usage" ] && echo ${usage}
exit 1
}

find -type f -regextype posix-egrep ...

find
type
egrep