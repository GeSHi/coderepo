<?php

echo strlen(file_get_contents(__FILE__));

$foo = array(
  '0' => 0123,
  '2' => 1E2,
);

// test
class dummy extends foo {
  static private $bar = 'asdf';
  const $zark = null;
  /**
   * @param foobar
   */
  function asdf($a, $s, $d = E_ALL) {
    $this->lockup = TRUE;
  	while {
  	  switch($d) {
  	    case E_STRICT:
  	      break;
  	  }
  	} do ($this->lockup);
  }
}

